{
  "name": "<%= projectName %>",
  "version": "0.0.0",
  "description": "Projects description",
  "author": "Authors name",
  "license": "CC",
  "repository": {
    "type": "git",
    "url": "git://repo/here.git"
  },
  "devDependencies": {
    "grunt": "^0.4.5",<% if (includeAutoprefixer) { %>
    "grunt-autoprefixer": "^2.2.0",<% } if (includeJade) { %>
    "grunt-contrib-jade": "^0.14.1",<% } %>
    "grunt-contrib-jshint": "^0.11.1",
    "grunt-contrib-sass": "^0.9.2",
    "grunt-contrib-uglify": "^0.8.0",
    "grunt-contrib-watch": "^0.6.1"
  }
}
